# Pinned exact (not >=) since 0.31.0 — reproducible builds. An unpinned
# paho-mqtt>=1.6.1 silently resolved to 2.1.0 and started emitting
# "Callback API version 1 is deprecated"; API v1 disappears entirely in
# paho 3.x, which would break the add-on on a routine rebuild with zero
# code change on our side. Bump deliberately, re-test, re-pin.
requests==2.34.2
beautifulsoup4==4.15.0
paho-mqtt==2.1.0
APScheduler==3.11.3
python-dateutil==2.9.0.post0
tzlocal==5.4.4
Flask==3.1.3
pypdf==6.14.2
websocket-client==1.9.0
waitress==3.0.2
